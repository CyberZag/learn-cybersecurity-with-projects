# Security Tools

Small Python/CLI utilities for log parsing, data transformation, and packet/data analysis.

## Tools

- `log_parser.py` — parses common log formats (syslog/CSV-style) and extracts fields of interest (IP, user, event, timestamp) for quick triage.

## Usage

Each script includes a `--help` flag and a docstring explaining inputs/outputs.
